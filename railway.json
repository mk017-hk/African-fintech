{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "infra/docker/all-in-one.Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'pnpm --filter @afristable/database prisma migrate deploy && (pnpm --filter @afristable/api tsx src/server.ts &) && pnpm --filter @afristable/web start -p ${PORT:-3000}'",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
